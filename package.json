{
  "name": "bigtube",
  "version": "4.1.0",
  "description": "Expands Youtube video to browser size",
  "scripts": {
    "build:chrome": "node build.mjs --browser=chrome",
    "build:firefox": "node build.mjs --browser=firefox"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.7.0",
    "@parcel/core": "^2.7.0",
    "archiver": "^5.3.1",
    "copyfiles": "^2.4.1",
    "minimist": "^1.2.7",
    "rimraf": "^3.0.2",
    "web-ext": "^7.3.1"
  }
}
