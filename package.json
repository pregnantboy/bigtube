{
  "name": "bigtube",
  "version": "3.0.6",
  "description": "Expands Youtube video to browser size",
  "scripts": {
    "chrome": "gulp",
    "firefox": "gulp build && web-ext build -s ./dest --overwrite-dest",
    "firefox-test": "gulp build && web-ext run -s ./dest"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/preset-env": "^7.12.7",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-zip": "^4.0.0",
    "rimraf": "^2.7.1",
    "web-ext": "^5.4.0"
  }
}
